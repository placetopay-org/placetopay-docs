title: Authorization
type: object
description: Authentication data for using the service. This information is delivered by email.
properties:
  username:
    type: string
    description: Name provided by the application.
    example: REPLACE_USERNAME_VALUE
  secret:
    type: string
    description: Key provided by the application.
    example: REPLACE_YOUR_SECRET_VALUE
required:
  - username
  - secret