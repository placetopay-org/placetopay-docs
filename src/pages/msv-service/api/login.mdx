import { HeroPattern } from '@/components/HeroPattern'

export const apiAssetPath = '/msv-service'
export const apiRefs = ['/api/login']
export const description = 'Obtén un token de acceso para autenticar tus peticiones a la API de MSV Service.'

<HeroPattern />

## Login {{ id: 'create-a-session', tag: 'POST', label: '/api/login' }}

Este endpoint permite obtener un token para realizar las peticiones a la API de MSV Service.

<Row>
  <Col>
    <ApiReader
      path="/api/login"
      method="post"
      api={props.refs}
    />
  </Col>
  <Col sticky>
    <CodeGroup title="Solicitud" tag="POST" label="/api/login">

```bash {{ title: 'Login MSV Service' }}
curl -X "POST" {{baseURl}}/api/login \
  -H "Content-Type: application/json" \
  -d '{
    "username": "RestService",
    "password": "password"
  }'
```
    </CodeGroup>
  </Col>
</Row>

<Row>
  <Col>
    <ApiReader
      path="/api/login"
      method="post"
      type="response"
      api={props.refs}
    />
  </Col>

  <Col sticky>
    <br />
    <CodeGroup title="Respuesta">
```json {{ title: '200' }}
{
  "status": {
    "status": "OK",
    "reason": null,
    "message": null,
    "date": "2022-08-16T13:12:18+00:00"
  },
  "access_token": "example-access-token",
  "secret_token": "example-secret-token"
}
```
```json {{ title: '401' }}
{
  "status": {
    "status": "FAILED",
    "reason": null,
    "message": "These credentials do not match our records.",
    "date": "2022-08-16T13:27:04+00:00"
  }
}
```
```json {{ title: '422' }}
{
  "status": {
    "status": "FAILED",
    "reason": null,
    "message": "The username field is required.",
    "date": "2022-08-16T13:22:18+00:00"
  },
  "errors": {
    "username": [
      "The username field is required."
    ]
  }
}
```
    </CodeGroup>
  </Col>
</Row>