
import { HeroPattern } from '@/components/HeroPattern';
import { useState } from 'react';
 
export default function ImageZoomGallery() {
  // Estado para manejar el zoom y la imagen activa
  const [isZoomed, setIsZoomed] = useState(false);
  const [activeImage, setActiveImage] = useState(null);
 
  // Función para alternar el zoom y establecer la imagen activa
  const toggleZoom = (imageSrc) => {
    setIsZoomed(!isZoomed);
    setActiveImage(imageSrc || null); // Si no hay imagen, limpiamos el estado
  };
 
  return (
    <div>
      <HeroPattern />

      <p className="lead">
       In the next view, you will have access to all the necessary options to share and manage the payment links you have previously created. These tools will allow you to ensure that your customers or recipients can easily receive the links.
      </p>


      <br></br>
      <h1> Share </h1>
      <p className="lead">
       In this section we are going to visualize the different ways to share our <b>payment link</b> created.
      </p>
      
      <h2>QR</h2>
      <p className="lead">
       In the recent view of the payment link created (see image above), the first option to share it is through the QR section. By clicking on this, you will be able to see the QR code already generated, which you can easily copy and share.      </p>

      <div className="flex justify-center">
          <div className="w-full sm:w-4/4 lg:w-2/3 sm:ml-10 sm:mr-6">
            <img
              src="/Qr_link.webp"
              alt="Qr_link"
              className="w-full h-auto shadow rounded-md my-0 transition-transform duration-300 ease-in-out cursor-pointer hover:scale-110 focus:scale-110"
              tabIndex={0}
              onClick={() => toggleZoom('/Qr_link.webp')}
            />
          </div>
      </div>
      <br></br>
      <h1>URL, Email, WhatsApp</h1>
      <p className="lead">
       The second option is to click on the three dots located in the <b>upper right corner</b> of the screen. Then, select the Share option to send the payment link easily.
       </p>

      <div className="flex justify-center">
          <div className="w-full sm:w-4/4 lg:w-2/3 sm:ml-10 sm:mr-6">
            <img
              src="/Share_link.webp"
              alt="Share_link"
              className="w-full h-auto shadow rounded-md my-0 transition-transform duration-300 ease-in-out cursor-pointer hover:scale-110 focus:scale-110"
              tabIndex={0}
              onClick={() => toggleZoom('/Share_link.webp')}
            />
          </div>
      </div>

      <p className="lead">
       Once clicked, a pop-up window will open where you will be able to see three options to share it. <b>(URL, Email, WhatsApp)</b>
      </p>

      <p className="lead">
       The first option displays the <b>URL</b> of the payment link which can be copied and pasted.
      </p>

      <div className="flex justify-center">
          <div className="w-full sm:w-4/4 lg:w-2/3 sm:ml-10 sm:mr-6">
            <img
              src="/Url_link.webp"
              alt="Url_link"
              className="w-full h-auto shadow rounded-md my-0 transition-transform duration-300 ease-in-out cursor-pointer hover:scale-110 focus:scale-110"
              tabIndex={0}
              onClick={() => toggleZoom('/Url_link.webp')}
            />
          </div>
      </div>

      <p className="lead">
       The second option allows you to add an additional email, once added you can click on the <b>send</b> button and it will be sent to the added emails.
      </p>

      <div className="flex justify-center">
          <div className="w-full sm:w-4/4 lg:w-2/3 sm:ml-10 sm:mr-6">
            <img
              src="/Email_link.webp"
              alt="Email_link"
              className="w-full h-auto shadow rounded-md my-0 transition-transform duration-300 ease-in-out cursor-pointer hover:scale-110 focus:scale-110"
              tabIndex={0}
              onClick={() => toggleZoom('/Email_link.webp')}
            />
          </div>
      </div>

      <p className="lead">
       Finally, you have the option to send via WhatsApp message, there you must type the mobile number to which you want to send the message and it will be sent. Sending by WhatsApp is subject to the country settings, which is why we recommend using the first 2 as a priority. 
      </p>

      <div className="flex justify-center">
          <div className="w-full sm:w-4/4 lg:w-2/3 sm:ml-10 sm:mr-6">
            <img
              src="/Wp_link.webp"
              alt="Wp_link"
              className="w-full h-auto shadow rounded-md my-0 transition-transform duration-300 ease-in-out cursor-pointer hover:scale-110 focus:scale-110"
              tabIndex={0}
              onClick={() => toggleZoom('/Wp_link.webp')}
            />
          </div>
      </div>
      

      <p className="lead">
       If you wish to reshare this <b>payment link</b>, in case it has not reached the customer, go to the related emails section and click on the resend email button, then you can see if the customer has seen it or not.      </p>

      <Note> Keep in mind that the link generated to share the payment link can be added to any social network.</Note>

      <h1>Related payments</h1>

      <p className="lead">
      At the end of the whole process, in the view corresponding to the payment link created, a detailed record of the transaction will be automatically generated in the Related Payments section. In this section, you will be able to view all the associated payments that have been processed through that specific payment link.      </p>

      <Note>Please note that the related payment shows only basic information. To view full details of payments received, it is recommended to view them from the administrative panel.</Note>
      
      <p className="lead">
       In case you do not see any expected record, it is important to verify that the link has been used correctly and that the payment flow has been completed without interruptions.     
     </p>

      <h1>Search for payment links</h1>

      <p className="lead">
       To search in the Payment Links history, go to the left side menu of the platform and select the option <b>Payment Links</b>. Then click on <b>Consultar</b> para visualizar el historial basado en el filtro preestablecido, que corresponde a la <b>fecha de creación</b>.
      </p>

      <p className="lead">
       If you want to make a more specific search, you can remove the default filter and use the option <b>Add filter</b>. This will allow you to configure customized criteria according to the available options, displaying only the information that fits the selected parameters. This way, you will have a more accurate and efficient access to the data of interest.
      </p>

      <div className="flex justify-center">
          <div className="w-full sm:w-4/4 lg:w-2/3 sm:ml-10 sm:mr-6">
            <img
              src="/Payments/searchPaymentLink.webp"
              alt="Search payment links"
              className="w-full h-auto shadow rounded-md my-0 transition-transform duration-300 ease-in-out cursor-pointer hover:scale-110 focus:scale-110"
              tabIndex={0}
              onClick={() => toggleZoom('/Payments/searchPaymentLink.webp')}
            />
          </div>
      </div>
      
  
      {/* Más imágenes... */}
 
      {/* Modal para la imagen ampliada */}
      {isZoomed && activeImage && (
        <div
          className="fixed inset-0 flex items-center justify-center bg-black bg-opacity-75 z-50"
          onClick={() => toggleZoom(null)}
        >
          <img
            src={activeImage}
            alt="Zoomed view"
            className="max-w-full max-h-[90vh] sm:max-w-5xl sm:max-h-[80vh] md:max-w-6xl md:max-h-[75vh] lg:max-w-7xl lg:max-h-[70vh] rounded-md shadow-lg"
          />
        </div>
      )}
     <p className="lead">    
In the following view, you will have access to all the necessary options<b> for the checkout process, as displayed in the forwarded email, payment link preview and the WebCheckout process</b>. These tools will allow you to ensure that your customers or recipients can easily receive the links.
</p>
    </div>
  );
}