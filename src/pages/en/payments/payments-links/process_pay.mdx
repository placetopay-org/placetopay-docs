import { HeroPattern } from '@/components/HeroPattern';
import { useState } from 'react';
 
export default function ImageZoomGallery() {
  // Estado para manejar el zoom y la imagen activa
  const [isZoomed, setIsZoomed] = useState(false);
  const [activeImage, setActiveImage] = useState(null);
 
  // Función para alternar el zoom y establecer la imagen activa
  const toggleZoom = (imageSrc) => {
    setIsZoomed(!isZoomed);
    setActiveImage(imageSrc || null); // Si no hay imagen, limpiamos el estado
  };
 
  return (
    <div>
      <HeroPattern />
      <h1>Payment process</h1>
      <p className="lead">
        Here you will find everything you need to manage your payments easily and efficiently. We offer you a step-by-step guide to complete your transactions, check their status and access the history in a clear and simple way.      </p>
 
      <h2>Email</h2>
      <p className="lead">
        Once the link has been shared via e-mail, the customer will be able to view it as follows, as illustrated below:
      </p>
      <div className="flex justify-center">
 
        <div className="w-full sm:w-4/4 lg:w-2/3 sm:ml-10 sm:mr-6 flex justify-center">
          <img
            src="/Email_pay.webp"
            alt="Email pay"
            className="w-1/3 h-auto shadow rounded-md my-0 transition-transform duration-300 ease-in-out cursor-pointer hover:scale-110 focus:scale-110"
            tabIndex={0}
          />
        </div>
      </div>
      <p className="lead">
        Through the content that arrives in the mail, transactions can be made quickly. There is a QR code to facilitate the payment process and the payment can be accessed through the link located at the bottom. In addition, a payment summary is included, such as the amount, reference and deadline date.
      </p>
      <Note>
          The URL included in the email is for one-time use only for the email sent. If you need to share the payment link with several people, remember to share it using the options mentioned above.
      </Note>
 
      <h2> Payment view</h2>
      <p className="lead">
        Upon opening the payment link, whether via email, QR code, direct URL or WhatsApp message, the following information will be displayed: the expiration date, the value to be paid, the merchant to which the payment will be made, the description of the payment, the reference and the status of the payment link.
      </p>
      <div className="flex justify-center">
        <div className="w-full sm:w-4/4 lg:w-2/3 sm:ml-10 sm:mr-6">
          <img
            src="/Payment_view.webp"
            alt="Payment view"
            className="w-full h-auto shadow rounded-md my-0 transition-transform duration-300 ease-in-out cursor-pointer hover:scale-110 focus:scale-110"
            tabIndex={0}
            onClick={() => toggleZoom('/Payment_view.webp')}
          />
        </div>
      </div>
 
      <h2>Webcheckout Process</h2>
      <p className="lead">
        Afterwards, the customer is redirected to the payment process (checkout), where he/she can view again the summary of the payment to be made, enter his/her e-mail address and click on the 'Continue' button.
      </p>
      <div className="flex justify-center">
        <div className="w-full sm:w-4/4 lg:w-2/3 sm:ml-10 sm:mr-6">
          <img
            src="/Checkout_page_view.webp"
            alt="Checkout page view"
            className="w-full h-auto shadow rounded-md my-0 transition-transform duration-300 ease-in-out cursor-pointer hover:scale-110 focus:scale-110"
            tabIndex={0}
            onClick={() => toggleZoom('/Checkout_page_view.webp')}
          />
        </div>
      </div>
 
      <p className="lead">
        After entering your email address and clicking 'Continue', you will proceed to enter your payment information and click on the 'Pay' button. It is important to note that, while in this section, there is an expiration time, which is indicated in the lower right corner.
      </p>
      <div className="flex justify-center">
        <div className="w-full sm:w-4/4 lg:w-2/3 sm:ml-10 sm:mr-6">
          <img
            src="/Checkout_page_view1.webp"
            alt="Checkout page view1"
            className="w-full h-auto shadow rounded-md my-0 transition-transform duration-300 ease-in-out cursor-pointer hover:scale-110 focus:scale-110"
            tabIndex={0}
            onClick={() => toggleZoom('/Checkout_page_view1.webp')}
          />
        </div>
      </div>
 
      <p className="lead">
        Once the payment process is completed, the customer displays the status of the transaction just completed.
      </p>
      <div className="flex justify-center">
        <div className="w-full sm:w-4/4 lg:w-2/3 sm:ml-10 sm:mr-6">
          <img
            src="/Checkout_page_view2.webp"
            alt="Checkout page view2"
            className="w-full h-auto shadow rounded-md my-0 transition-transform duration-300 ease-in-out cursor-pointer hover:scale-110 focus:scale-110"
            tabIndex={0}
            onClick={() => toggleZoom('/Checkout_page_view2.webp')}
          />
        </div>
      </div>
 
      <p className="lead">
        By clicking on the 'Back to Merchant' button, he will be redirected and will get the information of his payment voucher, including the transaction status, description, payment reference and date.
      </p>  
      <div className="flex justify-center">
        <div className="w-full sm:w-4/4 lg:w-2/3 sm:ml-10 sm:mr-6">
          <img
            src="/Checkout_page_returnurl.webp"
            alt="Checkout page returnurl"
            className="w-full h-auto shadow rounded-md my-0 transition-transform duration-300 ease-in-out cursor-pointer hover:scale-110 focus:scale-110"
            tabIndex={0}
            onClick={() => toggleZoom('/Checkout_page_returnurl.webp')}
          />
        </div>
      </div>
 
      <p className="lead">
        At the end of the whole process, in the view of the Link of payments created, a record of the transaction will be loaded in the related payments section, where all payments that have been executed through this payment link can be viewed.
      </p>  
      <div className="flex justify-center">
        <div className="w-full sm:w-4/4 lg:w-2/3 sm:ml-10 sm:mr-6">
          <img
            src="/View_pay_links.webp"
            alt="View pay links"
            className="w-full h-auto shadow rounded-md my-0 transition-transform duration-300 ease-in-out cursor-pointer hover:scale-110 focus:scale-110"
            tabIndex={0}
            onClick={() => toggleZoom('/View_pay_links.webp')}
          />
        </div>
      </div>
      <Note>
          Please note that the related payment includes only basic information. To view the details of payments received, it is recommended to do it from the administrative panel.
      </Note>
 
     
      {/* Más imágenes... */}
 
      {/* Modal para la imagen ampliada */}
      {isZoomed && activeImage && (
        <div
          className="fixed inset-0 flex items-center justify-center bg-black bg-opacity-75 z-50"
          onClick={() => toggleZoom(null)}
        >
          <img
            src={activeImage}
            alt="Zoomed view"
            className="max-w-full max-h-[90vh] sm:max-w-5xl sm:max-h-[80vh] md:max-w-6xl md:max-h-[75vh] lg:max-w-7xl lg:max-h-[70vh] rounded-md shadow-lg"
          />
        </div>
      )}
 
    </div>
  );
}