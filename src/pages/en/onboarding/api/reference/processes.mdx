export const sectionMode = 'nav';
export const title = 'Consultar un proceso'
export const description = 'Referencia del endpoint para consultar el estado de un proceso de Onboarding.'

export const apiRefs = ['/api/processes/{processId}'];

<ApiDownloads />

<MissingTranslationBanner />

## Consultar un proceso {{ id: 'show-a-process', tag: 'GET', label: '/api/processes/{processId}' }}

<Row>
  <Col>
    Devuelve el estado del proceso que devolvió una petición de escritura. Es la fuente de verdad del resultado, incluso cuando se registró una URL de notificación.

    Deja de consultar cuando la respuesta **deje de traer el header `Retry-After`**, no cuando veas un valor concreto de `status.status`.

    <ApiReader
      path="/api/processes/{processId}"
      type="params"
      api={props.refs}
    />

    <ApiReader
      path="/api/processes/{processId}"
      method="get"
      type="header"
      api={props.refs}
    />

  </Col>
  <Col sticky>
    <CodeGroup title="Solicitud" tag="GET" label="/api/processes/{processId}">

    ```bash {{ title: 'cURL' }}
    curl --request GET \
      --url '{URL_BASE}/api/processes/01kz0609xy2hd666mnx6b0jxpc' \
      --header 'Accept: application/json' \
      --header 'Authorization: Bearer {TU_TOKEN}'
    ```

    </CodeGroup>
  </Col>
</Row>

<Row>
  <Col>
    <ApiReader
      path="/api/processes/{processId}"
      method="get"
      type="response"
      api={props.refs}
    />
  </Col>
  <Col sticky>
    <CodeGroup title="Respuesta">

    ```json {{ title: 'En curso' }}
    {
      "status": {
        "status": "PENDING",
        "reason": null,
        "message": "The request is waiting to be processed.",
        "date": "2026-08-17T10:14:03-05:00"
      },
      "processId": "01kz0609xy2hd666mnx6b0jxpc",
      "action": "merchant.create",
      "result": null,
      "notification": null
    }
    ```

    ```json {{ title: 'Completado' }}
    {
      "status": {
        "status": "OK",
        "reason": null,
        "message": "The resource was created successfully.",
        "date": "2026-08-17T10:14:09-05:00"
      },
      "processId": "01kz0609xy2hd666mnx6b0jxpc",
      "action": "merchant.create",
      "result": {
        "merchantId": 18
      },
      "notification": {
        "url": "https://acme.example.com/hooks/onboarding",
        "state": "DELIVERED",
        "attempts": 1
      }
    }
    ```

    ```json {{ title: 'Fallido' }}
    {
      "status": {
        "status": "FAILED",
        "reason": "REQUIRED_FIELD_MISSING",
        "message": "One or more submitted fields were rejected by the registry.",
        "date": "2026-08-17T10:14:09-05:00"
      },
      "processId": "01kz0609xy2hd666mnx6b0jxpc",
      "action": "merchant.create",
      "result": null,
      "errors": {
        "name": ["This field is required by the registry."]
      },
      "notification": null
    }
    ```

    </CodeGroup>
  </Col>
</Row>
