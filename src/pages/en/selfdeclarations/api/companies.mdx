export const apiRefs = ['/api/v1/companies'];

# Companies - Create/Update

Allows you to create/update a company in the Self-Declaration system. Companies are any entities that must file taxes.

The company can be inactivated using the status parameter

Permissions required on the API user: manage-companies.api

<Row>
    <Col>
        <ApiReader
            path="/api/v1/companies"
            method="POST"
            api={props.refs}
        />
    </Col>
    <Col sticky>

         <br />
         <CodeGroup title="Request" tag="POST" label="/v1/companies">
            ```bash {{ title: 'cURL' }}
            curl --request POST \
            --url https://antioquia-dev-declarations.placetopay.ws/api/v1/companies \
            --header 'Accept: application/json' \
            --header 'Content-Type: application/json' \
            --header 'content-type: application/json' \
            --data '{
                "authorization": {
                    "username": "wG36MJpYEV"
                },
                "documentType": "NIT",
                "document": "900299228",
                "verification_digit": "",
                "legalName": "EGM Ingeniería Sin Fronteras",
                "commercialName": "PlacetoPay",
                "telephone": "4442310",
                "address": "Carrera 65 #45 - 20 Oficina 430",
                "email": "test@test.com",
                "classification": null,
                "country": "COL",
                "state": "05",
                "city": "001",
                "incomeEnable": {
                  "code": [
                    "CONSUMER_BEER"
                  ]
                }
            }'
          ```
        </CodeGroup>
    </Col>
</Row>

<Row>
    <Col>
        <ApiReader
            path="/api/v1/companies"
            method="post"
            type="response"
            api={props.refs}
        />
    </Col>

    <Col sticky>
        <br />
        <CodeGroup title="Response">
            ```json {{ title: 'Successful Response - Creation' }}
            {
                "status": "SUCCESS",
                "reason": "201",
                "companyId": 9,
                "message": "Empresa guardad@ correctamente."
            }
            ```
            ```json {{ title: 'Successful Response - Update' }}
            {
                "status": "SUCCESS",
                "reason": "200",
                "companyId": 9,
                "message": "Empresa guardad@ correctamente."
            }
            ```
        </CodeGroup>
    </Col>
</Row>
