export const description = 'detete-token.';

export const apiRefs = ['/api/tokens/delete'];

# Delete token

Its function is to eliminate the brand's token so that the consumer can
no longer use the token to create cryptograms and use them in subsequent
payments to different providers, nor can they query data about this token.
Before consuming this API, a merchant registered with the brand and a previously
assigned token must exist on the platform.

---



## Delete token {{ id: 'delete-token', tag: 'DELETE', label: '/api/tokens/delete' }}

<Row>
  <Col>
    <ApiReader
      path="/api/tokens/delete"
      method="DELETE"
      api={props.refs}
    />
  </Col>
  <Col sticky>

    <br />
    <CodeGroup title="Solicitud" tag="DELETE" label="/api/tokens/delete">
    ```bash {{ title: 'cURL' }}
    curl -X "DELETE" https://token-requestor.placetopay.ws/api/tokens/delete \
    -H "Content-Type: application/json" \
    -d '{
        "auth": {
            "login":"aabbccdd1234567890aabbccdd123456",
            "tranKey":"ABC123example456trankey+789abc012def3456ABC=",
            "nonce":"enQ4dXh3YWhkMWM=",
            "seed":"2023-06-21T09:56:06-05:00"
        },
        "siteId": "2e988305-40bb-4e77-a992-c0dce3303db9",
        "instrument": {
            "brandToken": {
                "id": "0e477297-3291-4841-99fb-ae9df82c588b"
            }
        },
        "reason": "FRAUD",
        "description": "mensaje corto del detalle de la razón"
    }'
    ```
    </CodeGroup>
  </Col>
</Row>

<Row>
  <Col>
    <ApiReader
      path="/api/tokens/delete"
      method="DELETE"
      type="response"
      api={props.refs}
    />
  </Col>
  <Col sticky>
    <br />
    <CodeGroup title="Respuesta">
    ```json {{ title: '200' }}
    {
        "status": {
            "status": "OK",
            "reason": "00",
            "description": "The request has been successfully processed",
        }
    }
    ```
    </CodeGroup>
  </Col>
</Row>

