import { HeroPattern } from '@/components/HeroPattern'

export const apiAssetPath = '/msv-service'
export const apiRefs = ['/api/login']
export const description = 'Obtain an access token to authenticate your requests to the MSV Service API.'

<HeroPattern />

## Login {{ id: 'create-a-session', tag: 'POST', label: '/api/login' }}

This endpoint lets you obtain a token to authenticate requests to the MSV Service API.

<Note>
The access credential (access_token) is valid for 24 hours from the moment it is issued. After that period it stops working and a new one must be requested through the authentication endpoint.
</Note>

<Note>
Important: requesting a new credential immediately invalidates all previously issued credentials for the same user, regardless of whether they were still valid. For this reason, if your integration runs multiple instances or processes that request credentials independently, we recommend centralizing credential retrieval and renewal in a single point to avoid different processes requesting credentials separately and invalidating each other.
</Note>

<Row>
  <Col>
    <ApiReader
      path="/api/login"
      method="post"
      api={props.refs}
    />
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="POST" label="/api/login">

```bash {{ title: 'MSV Service Login' }}
curl -X "POST" {{baseURl}}/api/login \
  -H "Content-Type: application/json" \
  -d '{
    "username": "RestService",
    "password": "password"
  }'
```
    </CodeGroup>
  </Col>
</Row>

<Row>
  <Col>
    <ApiReader
      path="/api/login"
      method="post"
      type="response"
      api={props.refs}
    />
  </Col>

  <Col sticky>
    <br />
    <CodeGroup title="Response">
```json {{ title: '200' }}
{
  "status": {
    "status": "OK",
    "reason": null,
    "message": null,
    "date": "2022-08-16T13:12:18+00:00"
  },
  "access_token": "example-access-token",
  "secret_token": "example-secret-token"
}
```
```json {{ title: '401' }}
{
  "status": {
    "status": "FAILED",
    "reason": null,
    "message": "These credentials do not match our records.",
    "date": "2022-08-16T13:27:04+00:00"
  }
}
```
```json {{ title: '422' }}
{
  "status": {
    "status": "FAILED",
    "reason": null,
    "message": "The username field is required.",
    "date": "2022-08-16T13:22:18+00:00"
  },
  "errors": {
    "username": [
      "The username field is required."
    ]
  }
}
```
    </CodeGroup>
  </Col>
</Row>
