export const sectionMode = 'nav';
export const description =
  'Referencia del endpoint para verificar el token de acceso y consultar el consumidor al que pertenece.'

export const apiRefs = ['/api/user'];

---

## Consultar el consumidor {{ id: 'show-the-consumer', tag: 'GET', label: '/api/user' }}

<Row>
  <Col>
    Describe la aplicación a la que pertenece el token. Solo exige que el token sea válido, sin ningún permiso concreto, así que es la forma más simple de comprobar credenciales antes de empezar a integrar.

    <ApiReader
      path="/api/user"
      method="get"
      type="header"
      api={props.refs}
    />

  </Col>
  <Col sticky>
    <CodeGroup title="Solicitud" tag="GET" label="/api/user">

    ```bash {{ title: 'cURL' }}
    curl --request GET \
      --url '{URL_BASE}/api/user' \
      --header 'Accept: application/json' \
      --header 'Authorization: Bearer {TU_TOKEN}'
    ```

    </CodeGroup>
  </Col>
</Row>

<Row>
  <Col>
    <ApiReader
      path="/api/user"
      method="get"
      type="response"
      api={props.refs}
    />
  </Col>
  <Col sticky>
    <CodeGroup title="Respuesta">

    ```json {{ title: '200' }}
    {
      "status": {
        "status": "OK",
        "reason": null,
        "message": null,
        "date": "2026-08-17T10:14:03-05:00"
      },
      "id": 1,
      "username": "acme",
      "environment": "SANDBOX",
      "active": true
    }
    ```

    ```json {{ title: '401' }}
    {
      "status": {
        "status": "FAILED",
        "reason": 401,
        "message": "The access token is missing or invalid.",
        "date": "2026-08-17T10:14:03-05:00"
      }
    }
    ```

    </CodeGroup>
  </Col>
</Row>
