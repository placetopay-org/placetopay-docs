
import { HeroPattern } from '@/components/HeroPattern';
import { useState } from 'react';
 
export default function ImageZoomGallery() {
  // Estado para manejar el zoom y la imagen activa
  const [isZoomed, setIsZoomed] = useState(false);
  const [activeImage, setActiveImage] = useState(null);
 
  // Función para alternar el zoom y establecer la imagen activa
  const toggleZoom = (imageSrc) => {
    setIsZoomed(!isZoomed);
    setActiveImage(imageSrc || null); // Si no hay imagen, limpiamos el estado
  };
 
  return (
    <div>
      <HeroPattern />

      <p className="lead">
       En la siguiente vista, tendrá acceso a todas las opciones necesarias para compartir y gestionar los enlaces de pago que haya creado previamente. Estas herramientas le permitirán asegurarse de que sus clientes o destinatarios puedan recibir los enlaces de forma sencilla.
      </p>


      <br></br>
      <h1> Compartir </h1>
      <p className="lead">
       En este apartado vamos a visualizar las diferentes maneras de compartir nuestro <b>link de pago</b> creado.
      </p>
      
      <h2>QR</h2>
      <p className="lead">
       En la vista reciente del enlace de pago creado (ver imagen anterior), la primera opción para compartirlo es a través del apartado QR. Al hacer clic en este, podrá visualizar el código QR ya generado, el cual puede copiar y compartir fácilmente.
      </p>

      <div className="flex justify-center">
          <div className="w-full sm:w-4/4 lg:w-2/3 sm:ml-10 sm:mr-6">
            <img
              src="/Qr_link.webp"
              alt="Qr_link"
              className="w-full h-auto shadow rounded-md my-0 transition-transform duration-300 ease-in-out cursor-pointer hover:scale-110 focus:scale-110"
              tabIndex={0}
              onClick={() => toggleZoom('/Qr_link.webp')}
            />
          </div>
      </div>
      <br></br>
      <h1>URL, Correo, Whassapp</h1>
      <p className="lead">
       La segunda opción consiste en hacer clic en los tres puntos ubicados en la esquina <b>superior derecha</b> de la pantalla. Luego, seleccione la opción Compartir para enviar el enlace de pago de manera sencilla.
      </p>

      <div className="flex justify-center">
          <div className="w-full sm:w-4/4 lg:w-2/3 sm:ml-10 sm:mr-6">
            <img
              src="/Share_link.webp"
              alt="Share_link"
              className="w-full h-auto shadow rounded-md my-0 transition-transform duration-300 ease-in-out cursor-pointer hover:scale-110 focus:scale-110"
              tabIndex={0}
              onClick={() => toggleZoom('/Share_link.webp')}
            />
          </div>
      </div>

      <p className="lead">
       Una vez da clic, se abrirá una ventana emergente donde podrá visualizar tres opciones para compartirlo. <b>(URL, Correo, Whatsapp)</b>
      </p>

      <p className="lead">
       Como primera opcion se visualiza la **URL** del link de pago el cual podemos copiar y pegar
      </p>

      <div className="flex justify-center">
          <div className="w-full sm:w-4/4 lg:w-2/3 sm:ml-10 sm:mr-6">
            <img
              src="/Url_link.webp"
              alt="Url_link"
              className="w-full h-auto shadow rounded-md my-0 transition-transform duration-300 ease-in-out cursor-pointer hover:scale-110 focus:scale-110"
              tabIndex={0}
              onClick={() => toggleZoom('/Url_link.webp')}
            />
          </div>
      </div>

      <p className="lead">
       La segunda opcion permite agregar un correo adicional, una vez se agregue podra dar clic en el boton <b>enviar</b> y este se emitira a los correos agregados
      </p>

      <div className="flex justify-center">
          <div className="w-full sm:w-4/4 lg:w-2/3 sm:ml-10 sm:mr-6">
            <img
              src="/Email_link.webp"
              alt="Email_link"
              className="w-full h-auto shadow rounded-md my-0 transition-transform duration-300 ease-in-out cursor-pointer hover:scale-110 focus:scale-110"
              tabIndex={0}
              onClick={() => toggleZoom('/Email_link.webp')}
            />
          </div>
      </div>

      <p className="lead">
       Por último, se cuenta con la opción de enviar vía mensaje de WhatsApp, allí debe digitar el número móvil al cual desea remitir este y será enviado. El envío por Whatsapp está sujeto a las configuración por país, es por ello que recomendamos hacer uso prioritario de las 2 primeras. 
      </p>

      <div className="flex justify-center">
          <div className="w-full sm:w-4/4 lg:w-2/3 sm:ml-10 sm:mr-6">
            <img
              src="/Wp_link.webp"
              alt="Wp_link"
              className="w-full h-auto shadow rounded-md my-0 transition-transform duration-300 ease-in-out cursor-pointer hover:scale-110 focus:scale-110"
              tabIndex={0}
              onClick={() => toggleZoom('/Wp_link.webp')}
            />
          </div>
      </div>
      

      <p className="lead">
       Si se desea compartir nuevamente este <b>link de pago</b>, en caso de no haberle llegado al cliente, se debe ir al apartado correos relacionados y se da clic en el botón reenviar correo, a continuación, se puede visualizar si el cliente lo ha visto o no.
      </p>

      <Note> Ten presente que el enlace generado para compartir el link de pago se puede agregar a cualquier red social</Note>

      <h1>Pagos relacionados</h1>

      <p className="lead">
      Al finalizar todo el proceso, en la vista correspondiente al link de pagos creado, se generará automáticamente un registro detallado de la transacción en el apartado Pagos relacionados. En esta sección, se podrán visualizar todos los pagos asociados que hayan sido procesados a través de ese link de pagos específico.
      </p>

      <Note>Ten en cuenta que el pago relacionado muestra solo información básica. Para ver detalles completos de los pagos recibidos, se recomienda consultarlos desde el panel administrativo.</Note>
      
      <p className="lead">
       En caso de no visualizar algún registro esperado, es importante verificar que el link haya sido utilizado correctamente y que el flujo de pago se haya completado sin interrupciones.
      </p>

      <h1>Buscar links de pago</h1>

      <p className="lead">
       Para buscar en el historial de Links de Pagos, dirígete al menú lateral izquierdo de la plataforma y selecciona la opción <b>Links de Pagos</b>. Luego, haz clic en <b>Consultar</b> para visualizar el historial basado en el filtro preestablecido, que corresponde a la <b>fecha de creación</b>.
      </p>

      <p className="lead">
       Si deseas realizar una búsqueda más específica, puedes eliminar el filtro predeterminado y utilizar la opción <b>Agregar filtro</b>. Esto te permitirá configurar criterios personalizados según las opciones disponibles, mostrando únicamente la información que se ajuste a los parámetros seleccionados. De esta forma, tendrás un acceso más preciso y eficiente a los datos de interés.
      </p>

      <div className="flex justify-center">
          <div className="w-full sm:w-4/4 lg:w-2/3 sm:ml-10 sm:mr-6">
            <img
              src="/Payments/searchPaymentLink.webp"
              alt="Search payment links"
              className="w-full h-auto shadow rounded-md my-0 transition-transform duration-300 ease-in-out cursor-pointer hover:scale-110 focus:scale-110"
              tabIndex={0}
              onClick={() => toggleZoom('/Payments/searchPaymentLink.webp')}
            />
          </div>
      </div>
      
  
      {/* Más imágenes... */}
 
      {/* Modal para la imagen ampliada */}
      {isZoomed && activeImage && (
        <div
          className="fixed inset-0 flex items-center justify-center bg-black bg-opacity-75 z-50"
          onClick={() => toggleZoom(null)}
        >
          <img
            src={activeImage}
            alt="Zoomed view"
            className="max-w-full max-h-[90vh] sm:max-w-5xl sm:max-h-[80vh] md:max-w-6xl md:max-h-[75vh] lg:max-w-7xl lg:max-h-[70vh] rounded-md shadow-lg"
          />
        </div>
      )}
 
    </div>
  );
}