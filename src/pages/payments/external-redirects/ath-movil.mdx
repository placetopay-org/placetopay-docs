import athMovil from '@/images/cards/ath_movil.svg'
import { PageTitle } from '@/components/PageTitle'
import { CountryFlags } from '@/components/Flags'
import { CodeGroup } from '@/components/mdx'

export const description = 'Integra ATH Móvil en Puerto Rico mediante Gateway, con ejemplos REST, consulta de estado y casos de prueba en sandbox.'

<PageTitle title='ATH Móvil' imageSrc={athMovil}/>
<CountryFlags countries="PR" prefix="Disponible para: " className='mt-4'/>

ATH Móvil permite que el pagador autorice un cobro desde la aplicación instalada en su teléfono. El pago se procesa de forma asíncrona: la respuesta inicial normalmente queda en estado `PENDING` y luego se actualiza con la decisión del pagador.

## Flujo de pago {{ id: 'payment-flow' }}

1. El comercio procesa una transacción mediante la API REST de Gateway.
2. El pagador recibe la solicitud de cobro y la aprueba o rechaza desde ATH Móvil.
3. La transacción permanece en `PENDING` mientras espera la decisión.
4. Placetopay consulta el resultado y actualiza la transacción.
5. El comercio obtiene el estado final consultando la referencia interna de Gateway.

![Flujo de pago ATH Móvil](/payments/ath-movil/pm-ath-movil-flow.png)

## Requisitos previos {{ id: 'requirements' }}

- Solicita la habilitación de ATH Móvil para el sitio de Placetopay que realizará los cobros.
- El comercio debe tener configurado su código de categoría comercial (MCC).
- Procesa los pagos en `USD`.
- Usa el código `ATHMV` para identificar el medio de pago.
- Envía el teléfono del pagador con el código internacional de Puerto Rico, por ejemplo `+1787...` o `+1939...`.

## Integración REST por Gateway {{ id: 'gateway-integration' }}

La imagen anterior muestra la experiencia del pagador. La implementación del comercio utiliza únicamente los endpoints REST de información, procesamiento y consulta de transacción.

### Secuencia de implementación {{ id: 'implementation-sequence' }}

1. Consulta las capacidades de ATH Móvil mediante `POST /gateway/information`.
2. Envía el cobro mediante `POST /gateway/process`.
3. Conserva el `internalReference` de la respuesta inicial.
4. Consulta `POST /gateway/query` mientras el estado sea `PENDING` y detente al recibir un estado final o alcanzar la expiración.

Los ejemplos cURL de las siguientes secciones muestran cómo construir y enviar cada petición REST. Las respuestas muestran los datos que el comercio debe procesar en cada paso.

### Consultar información {{ id: 'gateway-information', tag: 'POST', label: '/gateway/information' }}

<Row>
  <Col>
    El endpoint de [información](/gateway/api/reference/information#information-request) permite consultar las capacidades del servicio antes de procesar el cobro. Para ATH Móvil, envía el teléfono del pagador y el instrumento de redirección `ATHMV`.

    <Properties classNames="my-6">
      <Property name="auth" type="Authentication">
        Credenciales de autenticación del sitio.
      </Property>
      <Property name="locale" type="string" isRequired={false}>
        Idioma de la respuesta, por ejemplo `es_CO`.
      </Property>
      <Property name="payer.mobile" type="string">
        Teléfono del pagador con el prefijo internacional `+1`.
      </Property>
      <Property name="payment.reference" type="string">
        Referencia única del pago en el comercio.
      </Property>
      <Property name="payment.amount.currency" type="string">
        Moneda del pago. Para ATH Móvil debe ser `USD`.
      </Property>
      <Property name="payment.amount.total" type="number">
        Valor total del cobro, mayor que cero.
      </Property>
      <Property name="instrument.redirection.paymentMethod" type="string">
        Código del medio de pago. Debe ser `ATHMV`.
      </Property>
      <Property name="ipAddress" type="string" isRequired={false}>
        Dirección IP desde la que se origina la solicitud.
      </Property>
      <Property name="userAgent" type="string" isRequired={false}>
        Agente de usuario desde el que se origina la solicitud.
      </Property>
    </Properties>
  </Col>

  <Col sticky>
    <CodeGroup title="Solicitud" tag="POST" label="/gateway/information">
```bash {{ title: 'cURL' }}
curl -X "POST" https://api-co-dev.placetopay.ws/gateway/information \
  -H "Content-Type: application/json" \
  -d '{
    "locale": "es_CO",
    "auth": {
      "login": "aabbccdd1234567890aabbccdd123456",
      "tranKey": "ABC123example456trankey+789abc012def3456ABC=",
      "nonce": "NjE0OWVkODgwYjNhNw==",
      "seed": "2026-07-31T10:15:30-04:00"
    },
    "payer": {
      "name": "John",
      "surname": "Doe",
      "email": "john.doe@example.com",
      "mobile": "+19391111114"
    },
    "payment": {
      "reference": "ATHMV-ORDER-1001",
      "description": "Compra de prueba",
      "amount": {
        "currency": "USD",
        "total": 10
      }
    },
    "instrument": {
      "redirection": {
        "paymentMethod": "ATHMV"
      }
    },
    "ipAddress": "127.0.0.1",
    "userAgent": "Placetopay Sandbox"
  }'
```
    </CodeGroup>
  </Col>
</Row>

<Row>
  <Col>
    `requireRedirection: true` identifica a ATH Móvil como un servicio de autorización externa. No requiere datos de tarjeta, CVV ni OTP dentro de Gateway.

    <Properties classNames="my-6">
      <Property name="status" type="Status">
        Resultado general de la solicitud de información.
      </Property>
      <Property name="provider" type="string">
        Proveedor que procesará la operación: `ATH-MOVIL`.
      </Property>
      <Property name="serviceCode" type="string">
        Código del servicio habilitado: `ATHMV`.
      </Property>
      <Property name="requireRedirection" type="boolean">
        Indica que la autorización ocurre en un aplicativo externo.
      </Property>
      <Property name="requireOtp" type="boolean">
        Para ATH Móvil se devuelve `false`.
      </Property>
      <Property name="requireCvv2" type="boolean">
        Para ATH Móvil se devuelve `false`.
      </Property>
      <Property name="threeDS" type="string">
        Para ATH Móvil se devuelve `unsupported`.
      </Property>
      <Property name="credits" type="array">
        Opciones de crédito disponibles; para ATH Móvil se devuelve vacío.
      </Property>
    </Properties>
  </Col>

  <Col sticky>
    <br />
    <CodeGroup title="Respuesta">
```json {{ title: '200' }}
{
  "status": {
    "status": "OK",
    "reason": "00",
    "message": "La petición se ha procesado correctamente",
    "date": "2026-07-31T10:15:31-04:00"
  },
  "provider": "ATH-MOVIL",
  "serviceCode": "ATHMV",
  "requireRedirection": true,
  "cardType": null,
  "cardTypes": null,
  "displayInterest": false,
  "requireOtp": false,
  "requireCvv2": false,
  "threeDS": "unsupported",
  "credits": []
}
```
    </CodeGroup>
  </Col>
</Row>

### Procesar una transacción {{ id: 'gateway-process', tag: 'POST', label: '/gateway/process' }}

<Row>
  <Col>
    Envía el cobro al endpoint de [procesamiento de transacción](/gateway/api/reference/transaction#process-request). `payer.mobile` y `instrument.redirection.paymentMethod` son los campos particulares del flujo ATH Móvil.

    <Properties classNames="my-6">
      <Property name="auth" type="Authentication">
        Credenciales de autenticación del sitio.
      </Property>
      <Property name="locale" type="string" isRequired={false}>
        Idioma de la respuesta, por ejemplo `es_CO`.
      </Property>
      <Property name="payer" type="Person">
        Datos del pagador. `mobile` es obligatorio para ATH Móvil y debe incluir `+1`.
      </Property>
      <Property name="payment.reference" type="string">
        Referencia única del pago en el comercio.
      </Property>
      <Property name="payment.description" type="string" isRequired={false}>
        Descripción del pago.
      </Property>
      <Property name="payment.amount.currency" type="string">
        Moneda del pago. Para ATH Móvil debe ser `USD`.
      </Property>
      <Property name="payment.amount.total" type="number">
        Valor total del cobro, mayor que cero.
      </Property>
      <Property name="instrument.redirection.paymentMethod" type="string">
        Código del medio de pago. Debe ser `ATHMV`.
      </Property>
      <Property name="ipAddress" type="string" isRequired={false}>
        Dirección IP desde la que se origina la solicitud.
      </Property>
      <Property name="userAgent" type="string" isRequired={false}>
        Agente de usuario desde el que se origina la solicitud.
      </Property>
    </Properties>
  </Col>

  <Col sticky>
    <CodeGroup title="Solicitud" tag="POST" label="/gateway/process">
```bash {{ title: 'cURL' }}
curl -X "POST" https://api-co-dev.placetopay.ws/gateway/process \
  -H "Content-Type: application/json" \
  -d '{
    "locale": "es_CO",
    "auth": {
      "login": "aabbccdd1234567890aabbccdd123456",
      "tranKey": "ABC123example456trankey+789abc012def3456ABC=",
      "nonce": "NjE0OWVkODgwYjNhNw==",
      "seed": "2026-07-31T10:15:30-04:00"
    },
    "payer": {
      "name": "John",
      "surname": "Doe",
      "email": "john.doe@example.com",
      "mobile": "+19391111114"
    },
    "payment": {
      "reference": "ATHMV-ORDER-1001",
      "description": "Compra de prueba",
      "amount": {
        "currency": "USD",
        "total": 10
      }
    },
    "instrument": {
      "redirection": {
        "paymentMethod": "ATHMV"
      }
    },
    "ipAddress": "127.0.0.1",
    "userAgent": "Placetopay Sandbox"
  }'
```
    </CodeGroup>
  </Col>
</Row>

<Row>
  <Col>
    La respuesta inicial normalmente es `PENDING`. Conserva `internalReference`, porque es el identificador requerido para consultar el resultado.

    `redirection.additional.expiration` indica hasta cuándo puede confirmarse el pago. `redirection.processURL` se devuelve únicamente en sandbox para abrir el simulador; en producción normalmente es `null`.

    <Properties classNames="my-6">
      <Property name="status" type="Status">
        Estado inicial de la transacción; normalmente `PENDING`.
      </Property>
      <Property name="internalReference" type="number">
        Identificador que debe conservarse para consultar la transacción.
      </Property>
      <Property name="reference" type="string">
        Referencia enviada por el comercio.
      </Property>
      <Property name="paymentMethod" type="string">
        Código del medio de pago: `ATHMV`.
      </Property>
      <Property name="amount" type="Amount">
        Moneda y valor procesados.
      </Property>
      <Property name="provider" type="string">
        Proveedor que procesa la operación: `ATH-MOVIL`.
      </Property>
      <Property name="processorFields" type="object">
        Identificadores y códigos de respuesta entregados por el procesador.
      </Property>
      <Property name="redirection.processURL" type="string|null" isRequired={false}>
        URL del simulador. Solo se devuelve en sandbox.
      </Property>
      <Property name="redirection.additional.expiration" type="date-time">
        Fecha límite para confirmar el pago.
      </Property>
    </Properties>
  </Col>

  <Col sticky>
    <br />
    <CodeGroup title="Respuesta">
```json {{ title: 'Pendiente' }}
{
  "status": {
    "status": "PENDING",
    "reason": "?-",
    "message": "Transacción pendiente. Por favor consulte con su entidad financiera si el débito fue realizado",
    "date": "2026-07-31T10:15:31-04:00"
  },
  "date": "2026-07-31T10:15:31-04:00",
  "transactionDate": "2026-07-31T10:15:31-04:00",
  "internalReference": 485,
  "reference": "ATHMV-ORDER-1001",
  "paymentMethod": "ATHMV",
  "franchise": "athmv",
  "franchiseName": "ATH Móvil",
  "issuerName": null,
  "amount": {
    "currency": "USD",
    "total": 10
  },
  "conversion": {
    "from": {
      "currency": "USD",
      "total": 10
    },
    "to": {
      "currency": "USD",
      "total": 10
    },
    "factor": 1
  },
  "authorization": "000000",
  "receipt": null,
  "type": "AUTH_ONLY",
  "refunded": false,
  "lastDigits": null,
  "provider": "ATH-MOVIL",
  "discount": null,
  "processorFields": {
    "id": "63671893972dc2326463f93642910630",
    "b24": "?-",
    "paymentURL": "https://rest.test/mock/redirection/63671893972dc2326463f93642910630"
  },
  "additional": {
    "onTest": true
  },
  "redirection": {
    "paymentMethod": "ATHMV",
    "processURL": "https://rest.test/mock/redirection/63671893972dc2326463f93642910630",
    "additional": {
      "expiration": "2026-07-31T10:35:31-04:00"
    }
  }
}
```
    </CodeGroup>
  </Col>
</Row>

### Consultar el resultado {{ id: 'gateway-query', tag: 'POST', label: '/gateway/query' }}

<Row>
  <Col>
    Consulta el endpoint de [transacción por referencia interna](/gateway/api/reference/transaction#query-request) con el `internalReference` recibido al procesar el pago.

    <Properties classNames="my-6">
      <Property name="auth" type="Authentication">
        Credenciales de autenticación del sitio.
      </Property>
      <Property name="locale" type="string" isRequired={false}>
        Idioma de la respuesta, por ejemplo `es_CO`.
      </Property>
      <Property name="internalReference" type="number">
        Identificador recibido en la respuesta de `/gateway/process`.
      </Property>
    </Properties>
  </Col>

  <Col sticky>
    <CodeGroup title="Solicitud" tag="POST" label="/gateway/query">
```bash {{ title: 'cURL' }}
curl -X "POST" https://api-co-dev.placetopay.ws/gateway/query \
  -H "Content-Type: application/json" \
  -d '{
    "locale": "es_CO",
    "auth": {
      "login": "aabbccdd1234567890aabbccdd123456",
      "tranKey": "ABC123example456trankey+789abc012def3456ABC=",
      "nonce": "NjE0OWVkODgwYjNhNw==",
      "seed": "2026-07-31T10:16:00-04:00"
    },
    "internalReference": 485
  }'
```
    </CodeGroup>
  </Col>
</Row>

<Row>
  <Col>
    Mientras el estado sea `PENDING`, repite la consulta con un intervalo controlado. Detén las consultas cuando recibas `APPROVED`, `REJECTED` o `FAILED`, o cuando se alcance la expiración informada.

    Ante un timeout de consulta, conserva la transacción como pendiente y vuelve a consultar; no generes otro cobro automáticamente.

    <Properties classNames="my-6">
      <Property name="status" type="Status">
        Estado actual de la transacción: `PENDING`, `APPROVED`, `REJECTED` o `FAILED`.
      </Property>
      <Property name="internalReference" type="number">
        Identificador interno de la transacción.
      </Property>
      <Property name="reference" type="string">
        Referencia enviada por el comercio.
      </Property>
      <Property name="paymentMethod" type="string">
        Código del medio de pago: `ATHMV`.
      </Property>
      <Property name="amount" type="Amount">
        Moneda y valor procesados.
      </Property>
      <Property name="authorization" type="string|null">
        Código de autorización cuando el pago fue aprobado.
      </Property>
      <Property name="receipt" type="string|null">
        Comprobante entregado por el procesador, cuando aplique.
      </Property>
      <Property name="provider" type="string">
        Proveedor que procesó la operación: `ATH-MOVIL`.
      </Property>
      <Property name="redirection.additional.expiration" type="date-time">
        Fecha límite de confirmación de la sesión de pago.
      </Property>
    </Properties>
  </Col>

  <Col sticky>
    <br />
    <CodeGroup title="Respuesta">
```json {{ title: 'Aprobada' }}
{
  "status": {
    "status": "APPROVED",
    "reason": "00",
    "message": "Aprobada",
    "date": "2026-07-31T10:16:01-04:00"
  },
  "date": "2026-07-31T10:15:31-04:00",
  "transactionDate": "2026-07-31T10:16:01-04:00",
  "internalReference": 485,
  "reference": "ATHMV-ORDER-1001",
  "paymentMethod": "ATHMV",
  "franchise": "athmv",
  "franchiseName": "ATH Móvil",
  "issuerName": null,
  "amount": {
    "currency": "USD",
    "total": 10
  },
  "conversion": {
    "from": {
      "currency": "USD",
      "total": 10
    },
    "to": {
      "currency": "USD",
      "total": 10
    },
    "factor": 1
  },
  "authorization": "000000",
  "receipt": "426882",
  "type": "AUTH_ONLY",
  "refunded": false,
  "lastDigits": null,
  "provider": "ATH-MOVIL",
  "discount": null,
  "processorFields": {
    "id": "63671893972dc2326463f93642910630",
    "b24": "00",
    "paymentURL": "https://rest.test/mock/redirection/63671893972dc2326463f93642910630"
  },
  "additional": {
    "onTest": true
  },
  "redirection": {
    "paymentMethod": "ATHMV",
    "processURL": "https://rest.test/mock/redirection/63671893972dc2326463f93642910630",
    "additional": {
      "expiration": "2026-07-31T10:35:31-04:00"
    }
  }
}
```
    </CodeGroup>
  </Col>
</Row>

## Validaciones y consideraciones {{ id: 'validations' }}

| Campo o condición | Validación |
| --- | --- |
| `payment.amount.currency` | Debe ser `USD`. |
| `payment.amount.total` | Debe ser mayor que cero después de aplicar impuestos y detalles del monto. |
| `payer.mobile` | Es obligatorio para Gateway y debe incluir `+1` y el código de área de Puerto Rico. |
| `instrument.redirection.paymentMethod` | En Gateway debe ser exactamente `ATHMV`. |
| MCC del comercio | Debe estar configurado en el sitio antes de procesar transacciones. |
| Estado inicial | Una respuesta `PENDING` indica que el cobro fue recibido, no que fue aprobado. |
| `redirection.processURL` | Solo está disponible en sandbox; no debe ser requerido por la integración productiva. |

Las operaciones posteriores disponibles para una transacción dependen de su estado y de la configuración del sitio. Para reembolsos o reversos, consulta [Operaciones sobre una transacción](/gateway/api/reference/transaction#transaction-request).

## Casos de prueba en sandbox {{ id: 'sandbox-test-cases' }}

En sitios de prueba, el simulador determina el comportamiento a partir de `payer.mobile`. Envía siempre el prefijo internacional `+1`; sin él no se activará el caso esperado.

| Teléfono | Resultado esperado |
| --- | --- |
| `+19391111114` | La consulta finaliza en `APPROVED`. |
| `+19391111112` | La consulta finaliza en `REJECTED` por cancelación. |
| `+19391111113` | La consulta finaliza en `FAILED`. |
| `+19391111111` | La transacción permanece en `PENDING`. |
| `+19391111117` | La consulta finaliza en `FAILED` por token expirado. |
| `+19391111120` | La consulta simula un timeout y la transacción permanece en `PENDING`. |
| `+19391111178` | El procesamiento falla inmediatamente por token expirado. |

## FAQs {{ id: 'faqs' }}

<details>
<summary>¿Cómo configurar ATH Móvil en tu cuenta de Placetopay?</summary>
<p>Contacta a nuestro equipo de operaciones para habilitar esta funcionalidad en tus sitios a través del correo servicioposventa@placetopay.com.</p>
</details>

<details>
<summary>¿Debo redirigir al usuario en una integración Gateway?</summary>
<p>No en producción. El usuario aprueba el cobro desde la aplicación ATH Móvil y el comercio consulta el resultado con la referencia interna. La URL de redirección solo se entrega en sandbox para acceder al simulador.</p>
</details>
